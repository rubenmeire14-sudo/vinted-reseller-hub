export const metadata = {
  title: 'Vinted Reseller Hub',
  description: 'Webhook API en Dashboard',
};

export default function RootLayout({ children }) {
  return (
    <html lang="nl">
      <body>{children}</body>
    </html>
  );
}
